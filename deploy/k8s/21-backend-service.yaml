apiVersion: v1
kind: Service
metadata:
  name: novaapm-backend
  namespace: novaapm-system
  labels:
    app.kubernetes.io/name: novaapm-backend
    app.kubernetes.io/part-of: novaapm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: novaapm-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
